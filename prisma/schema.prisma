datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model poetryMaker {
  id          Int       @id @default(autoincrement())
  poem        String
  date        DateTime
  prompt  String
}